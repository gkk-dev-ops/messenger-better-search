name: Release

on:
  push:
    branches: ["master"]
    paths:
      - "package.json"
      - "manifest.json"
      - "src/**"
      - "scripts/**"
      - "tests/**"
      - ".github/workflows/release.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund

      - name: Read version
        id: version
        run: |
          VERSION=$(node -e "console.log(require('./package.json').version)")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Check whether release exists
        id: existing
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          if gh release view "v$VERSION" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Extension E2E
        run: xvfb-run -a npm run test:e2e

      - name: Package extension
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          cd dist
          zip -r "../messenger-better-search-v$VERSION.zip" .

      - name: Update existing release asset
        if: steps.existing.outputs.exists == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          gh release upload "v$VERSION" "messenger-better-search-v$VERSION.zip" --clobber

      - name: Publish GitHub Release
        if: steps.existing.outputs.exists == 'false'
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          gh release create "v$VERSION"             "messenger-better-search-v$VERSION.zip"             --target "$GITHUB_SHA"             --title "Messenger Better Search v$VERSION"             --notes "Packaged release of Messenger Better Search.

          Download the ZIP, unpack it, then open chrome://extensions, enable Developer mode and choose Load unpacked.

          Local-first conversation search with optional BYOK transcription, image context and semantic search."
